MOTORAPI_API_KEY=
DATABASE_URL=
APP_URL=

ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=

BOOKING_ADMIN_EMAIL=

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=
MAIL_FROM_NAME=CleanWash

# Optional: protects /api/cron/trustpilot-draw. If set, Vercel Cron must send
# an "Authorization: Bearer <value>" header matching this (set it as a Vercel
# Cron secret / env var). If unset, the endpoint is unauthenticated.
CRON_SECRET=
